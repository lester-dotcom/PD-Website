---
import { getEntry, render } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import PageHero from '../components/PageHero.astro';
import BlockRenderer from '../components/BlockRenderer.astro';
import { cornerAgency } from '../components/IconCluster.astro';

const page = await getEntry('pages', 'for-agencies');
if (!page) throw new Error('Missing pages/for-agencies.yaml content entry');
const { title, metaDescription, hero, blocks } = page.data;
---

<BaseLayout title={title} description={metaDescription}>
  <PageHero
    eyebrow={hero.eyebrow}
    title={hero.title}
    description={hero.description}
    cluster={cornerAgency}
  />

  <BlockRenderer blocks={blocks} />
</BaseLayout>
