---
import { getEntry } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageHero from '../../components/PageHero.astro';
import BlockRenderer from '../../components/BlockRenderer.astro';
import { cornerSocial } from '../../components/IconCluster.astro';

const page = await getEntry('pages', 'paid-social-other-platforms');
if (!page) throw new Error('Missing pages/paid-social-other-platforms.yaml content entry');
const { title, metaDescription, hero, blocks } = page.data;
---

<BaseLayout title={title} description={metaDescription}>
  <PageHero
    eyebrow={hero.eyebrow}
    title={hero.title}
    description={hero.description}
    cluster={cornerSocial}
    anchorLogo="linkedin"
  />

  <BlockRenderer blocks={blocks} />
</BaseLayout>
